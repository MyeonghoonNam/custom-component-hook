import ErrorText from '@components/ErrorText';

export default {
  title: 'Components/ErrorText',
  component: ErrorText,
};

export function Default() {
  return <ErrorText>Error Text</ErrorText>;
}
